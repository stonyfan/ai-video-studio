# samples/

小型示例素材目录（每个文件 < 5 MB，总 < 50 MB，可入仓库供测试）。

## 用途

- 端到端测试（pytest e2e）
- 回归测试基线
- 新用户快速验证安装

## 计划放入

- `mini_dao2/` - 檀道2 缩减版（3-5 段，每段 5-10s，720p）
- `mini_yulan/` - 玉兰花马丁尼缩减版（3-5 段，每段 5-10s，720p）
- `bgm/` - 30s 短 BGM 片段

不放：
- 4K 原素材（太大）
- 完整 30s 成片（用 outputs/）

阶段 1 启动时由开发者手动从原素材裁剪生成。
